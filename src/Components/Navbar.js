const Navbar=()=>{
    return(
        <nav className="navbar">
            <div>
                <a href="/">Home</a>
                <a href="/create">New Blog</a>
            </div>
        </nav>
    );
}

export default Navbar;
